package:
  name: oeommtools
  version: 0.0.0

source:
  path: ../../

build:
  preserve_egg_dir: True
  number: 0

requirements:
  build:
    - python
    - pytest
    - setuptools
    - numpy
    - numpydoc
    - openmm
  run:
    - python
    - pytest
    - setuptools
    - numpy
    - numpydoc
    - openmm

test:
  requires:
    - pytest
  imports:
    - oeommtools


about:
  home: https://github.com/oess/oeommtools